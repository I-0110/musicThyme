import { Josefin_Slab, Teachers } from 'next/font/google';

export const josefin = Josefin_Slab({ subsets: ['latin'] });

export const teachers = Teachers({
    weight: ['400', '700'],
    subsets: ['latin'],
});